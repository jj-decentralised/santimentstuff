[phases.setup]
nixPkgs = ["python311Full", "python311Packages.pip"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn products.smart_money.api:app --host 0.0.0.0 --port ${PORT:-8000}"
